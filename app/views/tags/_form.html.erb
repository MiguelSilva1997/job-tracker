<%= form_for @tag do |f| %>
  <%= f.label :title%>
  <%= f.text_field :title %><br>

  <%= hidden_field_tag "tag[job_ids][]", nil %>
  <% Job.all.each do |job|%>
    <%= check_box_tag "tag[job_ids][]", job.id,
     @tag.job_ids.include?(job.id), id: "job-#{tag.id}" %>
    <%= label_tag "job-#{job.id}", job.title %><br>
  <% end %>
  <%= f.submit %>
<% end %>
